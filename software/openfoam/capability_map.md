# OpenFOAM 能力地图

| 编号 | 能力名称 | 问题类型 | 当前证据 | 下一门禁 | 阻塞项 | 资产化目标 |
| --- | --- | --- | --- | --- | --- | --- |
| C01 | lid-driven cavity incompressible laminar flow | 二维不可压缩层流顶盖驱动方腔 | `benchmark_validated`，local `openfoam_com_v2112` WSL 四案例 integration benchmark | `double-v` | 缺外部中心线参考和网格/时间收敛误差表 | 输入 OpenFOAM distribution/runtime profile、Re、方腔尺寸、顶盖速度、运动黏度、网格分辨率、时间步长、离散格式和验收阈值；当前已有资产卡、run schema、case generator、solver execution、中心线 CSV、矩阵图、manifest.json、metrics.json、validation.json。 |
| C03 | backward-facing step RANS internal flow | 不可压缩 RANS 内流台阶分离和再附 | `benchmark_validated`，local `openfoam_com_v2112` WSL 四案例 integration benchmark，wall-shear/yPlus 为 Python proxy | `targeted-regression` 后再进入 `double-v` | local v2112 WSL profile functionObject `sha1` IO failure；缺 native postprocess parity 和外部 RANS reference | 输入 runtime profile、官方 pitzDaily template、入口速度、运动黏度、k/epsilon、网格尺度、稳态迭代控制、速度剖面站位和验收阈值；当前已有资产卡、schema、configs、template runner、simpleFoam runtime、pressure/velocity/wall-shear proxy CSV、矩阵图。 |
| C06 | dam break VOF free surface | 二维两相 VOF 自由表面瞬态流动 | `benchmark_validated`，local `openfoam_com_v2112` WSL 四案例短时域 integration benchmark，alpha/gauge 为 Python postprocess | `targeted-regression` 后再进入 `double-v` | local v2112 WSL profile sampling `sha1` IO failure；短时域；缺 full-horizon reference | 输入 runtime profile、官方 damBreak template、重力、水柱高度、网格尺度、时间控制、alpha 阈值、gauge 位置和验收阈值；当前已有资产卡、schema、configs、template runner、interFoam runtime、alpha boundedness、水体积、前缘、gauge CSV、矩阵图。 |
| C07 | conjugate heat transfer cooling | 固-流多区域稳态共轭传热冷却 | `benchmark_candidate`，local OpenFOAM.com v2112 WSL solver/tutorial/runtime-profile evidence | `static-readiness` | 缺 C07 run schema、baseline config、dry-run case generator、runner、postprocess、扰动矩阵和 validation report | 输入 runtime profile、regionProperties、热物性、热源、环境温度、网格/并行流程、稳态迭代控制和验证阈值；目标是从官方 cpuCabinet 模板生成多区域 CHT case，输出温度场、region 温度摘要、界面热平衡 proxy、metrics.json 和 validation.json |
