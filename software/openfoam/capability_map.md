# OpenFOAM 能力地图

| 编号 | 能力名称 | 问题类型 | 当前证据 | 下一门槛 | 阻塞项 | 资产化目标 |
| --- | --- | --- | --- | --- | --- | --- |
| C01 | lid-driven cavity incompressible laminar flow | 二维不可压缩层流顶盖驱动方腔 | `benchmark_validated`；local `openfoam_com_v2112` WSL 四案例 integration benchmark | `double-v` | 缺少外部中心线参考和网格/时间收敛误差表 | 输入 runtime profile、Re、方腔尺寸、顶盖速度、运动黏度、网格分辨率、时间步长、离散格式和验收阈值；输出中心线 CSV、矩阵图、manifest、metrics 和 validation。 |
| C03 | backward-facing step RANS internal flow | 不可压缩 RANS 内流台阶分离和再附 | `benchmark_validated`；local `openfoam_com_v2112` WSL 四案例 integration benchmark；wall-shear/yPlus 为 Python proxy | `targeted-regression` 后进入 `double-v` | local v2112 WSL profile functionObject `sha1` IO failure；缺 native postprocess parity 和外部 RANS reference | 输入 runtime profile、官方 pitzDaily template、入口速度、运动黏度、k/epsilon、网格尺度、稳态迭代控制、速度剖面站位和验收阈值；输出 pressure/velocity/wall-shear proxy CSV 和 validation。 |
| C05 | transient cylinder vortex shedding | 不可压缩层流瞬态圆柱绕流和涡街 | `validation_failed`；solver-only smoke 和 Python force proxy smoke 已通过；official template 几何已修正为 `D≈0.12m`、厚度 `≈0.015m`；修正几何后的 long-horizon run 完成并产出 320 行 force CSV，但 `St=0.13846` 未进入 `[0.16, 0.24]` | native/DMD/reference Strouhal parity 和 mesh/time-step sensitivity | native forceCoeffs 受 local v2112 `sha1` IO 阻断；Python proxy 不是 benchmark-grade force parity；当前 Strouhal target mismatch 尚未解释 | 输入圆柱直径、入口速度、运动黏度、Re、mesh workflow、time controls、PIMPLE controls、force extraction source 和 Strouhal 目标区间；输出 lift/drag time series、force coefficient CSV、Strouhal summary、metrics、validation。 |
| C06 | dam break VOF free surface | 二维两相 VOF 自由液面瞬态流动 | `benchmark_validated`；local `openfoam_com_v2112` WSL 四案例短时域 integration benchmark；alpha/gauge 为 Python postprocess | `targeted-regression` 后进入 `double-v` | local v2112 WSL profile sampling `sha1` IO failure；短时域；缺 full-horizon reference | 输入 runtime profile、官方 damBreak template、重力、水柱高度、网格尺度、时间控制、alpha 阈值、gauge 位置和验收阈值；输出 alpha boundedness、水体积、前缘、gauge CSV、矩阵图和 validation。 |
| C07 | conjugate heat transfer cooling | 固-流多区域稳态共轭传热冷却 | `benchmark_candidate`；cpuCabinet np=2 `realizableKE`/`kEpsilon` 均 Time=2 FPE；`multiRegionHeaterRadiation` packaged baseline + heater/airflow/mesh integration matrix 已通过 | native heat-flux parity 和 `double-v` | MHR 仍是短时 `Time=2` integration；patch heat-flux 为 Python owner-cell proxy，缺稳态收敛和外部参考 | 输入 runtime profile、regionProperties、热物性、固定温度/功率热源、airflow 边界、mesh cells、radiation view-factor 预处理、稳态迭代控制和验证阈值；输出温度场、region 温度摘要、界面温度 proxy、patch heat-flux proxy、metrics 和 validation。 |
