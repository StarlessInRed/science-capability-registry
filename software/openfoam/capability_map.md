# OpenFOAM 能力地图

本文件记录首批 OpenFOAM Capability Map C01-C08。状态以 capability card、runtime 证据和验证报告为准；没有本地 schema/config/package/runtime 证据的条目只能保持 `benchmark_candidate`。

| 编号 | capability slug | solver / tutorial | 问题类型 | 当前证据状态 | 必验指标 | 下一门槛 |
| --- | --- | --- | --- | --- | --- | --- |
| C01 | `lid_driven_cavity_incompressible_laminar` | `icoFoam` / lid-driven cavity | 二维不可压缩层流顶盖驱动方腔 | `benchmark_validated`。已有本地 OpenFOAM.com v2112 WSL integration matrix。 | residual、continuity、中心线速度、网格加密趋势 | `double-v`：补外部中心线参考和网格/时间收敛误差表 |
| C02 | `potential_flow_cylinder_analytical_validation` | `potentialFoam` / cylinder | 圆柱势流解析验证 | `package_skeleton_created`。已有本地 OpenFOAM.com v2112 tutorial source evidence、schema、baseline/mesh-refined config、package dry-run 和 analytical formula unit tests；尚无 solver runtime metrics。 | 解析解误差、表面 `Cp`、速度场、压力场 | local smoke 后进入 analytical double-v |
| C03 | `backward_facing_step_rans_internal_flow` | `simpleFoam` / `pitzDaily` | RANS 内流后台阶分离和再附 | `benchmark_validated`。已有本地 OpenFOAM.com v2112 WSL integration matrix；wall-shear/yPlus 仍是 Python proxy。 | 再附长度、压力降、`y+`、wall functions、residual | `targeted-regression`：native wallShearStress/yPlus parity 和外部 RANS reference |
| C04 | `external_aero_motorbike_rans_snappy` | `simpleFoam + snappyHexMesh` / `motorBike` | 外流气动 RANS 和几何贴体网格 | `benchmark_candidate`。本地 OpenFOAM.com v2112 存在 `incompressible/simpleFoam/motorBike` 官方 tutorial source evidence；尚无本仓库 schema/config/package/runtime。 | mesh quality、`Cd`/`Cl`、`forceCoeffs`、`y+`、residual | `static-readiness` 后进入 local mesh/solver smoke，再进入 integration |
| C05 | `transient_cylinder_vortex_shedding` | `pimpleFoam` 或 `pisoFoam` / cylinder | 瞬态圆柱绕流涡脱落 | `validation_failed`。solver-only smoke 与 Python force proxy smoke 通过；修正几何后的 long-horizon run 完成，但 `St=0.13846` 未进入目标区间 `[0.16, 0.24]`。 | Strouhal、`Cd`/`Cl` 时序、Courant、force history | native/DMD/reference Strouhal parity，补 mesh/time-step sensitivity |
| C06 | `dam_break_vof_free_surface` | `interFoam` / `damBreak` | VOF 溃坝自由液面瞬态两相流 | `benchmark_validated`。已有本地 OpenFOAM.com v2112 WSL short-horizon integration matrix；alpha/gauge 仍是 Python postprocess。 | alpha boundedness、水量守恒、液面前沿、probe pressure、Courant | `targeted-regression`：full-horizon、native sampling parity 和外部 dam-break reference |
| C07 | `conjugate_heat_transfer_cooling` | `chtMultiRegionFoam` family；当前 packaged baseline 为 `chtMultiRegionSimpleFoam/multiRegionHeaterRadiation` | 流固耦合传热冷却 | `benchmark_candidate`。已有 v2112 CHT solver/tutorial/profile evidence，MHR baseline 与 heater/airflow/mesh perturbation matrix 通过；仍是短时 integration 和 Python heat-flux proxy。 | 界面热流连续、能量平衡、温度范围、region 温度趋势 | native heat-flux parity、长时/稳态收敛、必要时补 `chtMultiRegionFoam` tutorial baseline |
| C08 | `compressible_shock_capturing_forward_step` | `rhoCentralFoam` 或等价 shock solver / `forwardStep` | 可压缩激波捕捉前台阶 | `package_skeleton_created`。已基于本地 OpenFOAM.com v2112 `compressible/rhoCentralFoam/forwardStep` 官方 tutorial source evidence 建立 schema/config/package dry-run 和 shock metric unit tests；尚无 runtime shock validation。 | 激波位置、压力/密度跳跃、CFL、质量/能量守恒 | shock-capturing smoke 后进入 double-v |
