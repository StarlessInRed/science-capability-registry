# OpenFOAM 能力地图

本文件记录首批 OpenFOAM Capability Map C01-C08。状态以 capability card、runtime 证据和验证报告为准；2026-07-01 起当前阶段采用 `case-freeze` 口径收敛：本地可执行、可复验、验证边界清楚即可结束首批 OpenFOAM 工作；`double-v`、外部 reference、长时稳态或网格/时间独立性作为后续增强，不再阻塞当前转向。

| 编号 | capability slug | solver / tutorial | 问题类型 | 当前证据状态 | 必验指标 | 下一门槛 |
| --- | --- | --- | --- | --- | --- | --- |
| C01 | `lid_driven_cavity_incompressible_laminar` | `icoFoam` / lid-driven cavity | 二维不可压缩层流顶盖驱动方腔 | `benchmark_validated`。已有本地 OpenFOAM.com v2112 WSL integration matrix。 | residual、continuity、中心线速度、网格加密趋势 | `double-v`：补外部中心线参考和网格/时间收敛误差表 |
| C02 | `potential_flow_cylinder_analytical_validation` | `potentialFoam` / cylinder | 圆柱势流解析验证 | `benchmark_candidate`。`blockMesh/potentialFoam` 可运行，finite-domain diagnostic 已通过 solver/artifact/finite-error extraction；严格无界圆柱解析误差和表面 `Cp` 仍不达标，因此本轮只做非推广性 case-freeze。 | 解析解误差、表面 `Cp`、速度场、压力场 | 后续若要推广：有限域修正参考、表面采样审查、domain-expanded convergence |
| C03 | `backward_facing_step_rans_internal_flow` | `simpleFoam` / `pitzDaily` | RANS 内流后台阶分离和再附 | `benchmark_validated`。已有本地 OpenFOAM.com v2112 WSL integration matrix；wall-shear/yPlus 仍是 Python proxy。 | 再附长度、压力降、`y+`、wall functions、residual | `targeted-regression`：native wallShearStress/yPlus parity 和外部 RANS reference |
| C04 | `external_aero_motorbike_rans_snappy` | `simpleFoam + snappyHexMesh` / `motorBike` | 外流气动 RANS 和几何贴体网格 | `benchmark_validated` for local case-freeze。v2412 coarse34-layer3 已通过 strict checkMesh、20-step simpleFoam、native forceCoeffs 和 finite native yPlus diagnostic；不声称 wall-function y+ band 合格。 | mesh quality、`Cd`/`Cl`、`forceCoeffs`、`y+`、residual | 后续若要推广：wall-normal layer/y+ band closure，Cd/Cl convergence/reference |
| C05 | `transient_cylinder_vortex_shedding` | `pimpleFoam` 或 `pisoFoam` / cylinder | 瞬态圆柱绕流涡脱落 | `benchmark_validated` for local case-freeze。v2412 native forceCoeffs long-horizon run 完成，finite-domain OpenFOAM cylinder2D tutorial 以 `St≈0.14` / `[0.13,0.15]` 闭环；外部 free-cylinder `[0.16,0.24]` 保留为后续推广边界。 | Strouhal、`Cd`/`Cl` 时序、Courant、force history | 后续若要推广：domain/mesh/time-step sensitivity 或 independent frequency extraction |
| C06 | `dam_break_vof_free_surface` | `interFoam` / `damBreak` | VOF 溃坝自由液面瞬态两相流 | `benchmark_validated`。已有本地 OpenFOAM.com v2112 WSL short-horizon integration matrix；v2412 sampleSets artifact 与 1.0 s full-horizon runtime 已通过。 | alpha boundedness、水量守恒、液面前沿、probe pressure、Courant | `double-v`：sampleSets VTP value parity 和外部 dam-break reference |
| C07 | `conjugate_heat_transfer_cooling` | `chtMultiRegionFoam` family；当前 packaged baseline 为 `chtMultiRegionSimpleFoam/multiRegionHeaterRadiation` | 流固耦合传热冷却 | `benchmark_validated` for local case-freeze。v2112 MHR baseline+perturbation matrix 通过；v2412 solver-postProcess native wallHeatFlux smoke 已写出五个 region 字段；`cpuCabinet` 保留为失败诊断路径。 | 界面热流连续、能量平衡、温度范围、region 温度趋势 | 后续若要推广：parsed native/reference heat-rate parity、长时/稳态收敛、必要时补 `chtMultiRegionFoam` tutorial baseline |
| C08 | `compressible_shock_capturing_forward_step` | `rhoCentralFoam` 或等价 shock solver / `forwardStep` | 可压缩激波捕捉前台阶 | `benchmark_validated` for local case-freeze。reduced-CFL runtime 已到 `Time=4`，max Courant、leading-shock jump sanity、local smoke reference、owner-cell boundary-flux proxy 与 face-field flux integration 均通过 smoke gate。 | 激波位置、压力/密度跳跃、CFL、质量/能量守恒 | 后续若要推广：外部或独立 shock reference；必要时补 native `rhoPhi/phi` flux parity |
