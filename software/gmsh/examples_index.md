# Gmsh examples_index

本文件登记已经转化或待转化为科学能力资产的 Gmsh 官方文档、tutorial、benchmark 和本地 case 来源。

| ID | 来源 | capability | domain | 资产卡 | benchmark 状态 | evidence scope | next gate | known blockers | 备注 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| C01 | Gmsh reference manual and official `t1.geo` tutorial | parametric geometry and mesh generation | meshing | `software/gmsh/assets/C01_parametric_geometry_mesh_generation.yaml` | package skeleton created | official documentation plus local schema/config/package dry-run contract, OpenFOAM import smoke, and potentialFoam solve smoke | mesh-quality perturbation and solver-ready regression | 缺少扰动矩阵、更完整 mesh-quality 回归和 benchmark 级 CFD 精度证据 | 已建立 `.geo` 生成、Python API runtime、mesh summary、validation contract、OpenFOAM.com v2112 `gmshToFoam` 导入和 `potentialFoam -writep` 求解 smoke；不提前提升为已验证能力。 |
| C02 | Gmsh physical groups and elementary entity documentation | boundary physical group contract | meshing | `software/gmsh/assets/C02_boundary_physical_group_contract.yaml` | benchmark candidate | physical group name/dimension/role contract candidate | schema + manifest validator + downstream boundary map smoke | 尚未实现 schema/package/runtime；需要 missing/renamed boundary negative tests | 作为 C03/C05/C06 的边界语义底座，避免下游 solver 依赖隐式 patch 名称。 |
| C03 | Gmsh mesh-size fields, algorithms, and quality controls | mesh refinement quality trend | meshing | `software/gmsh/assets/C03_mesh_refinement_quality_trend.yaml` | benchmark candidate | refinement matrix and quality-trend candidate | coarse/baseline/fine configs + quality parser + trend report | 尚未实现 refinement matrix；缺少 low-quality 和 nonfinite negative tests | 先验证 mesh 本身，再交给 solver；不把 solver 物理误差塞进 Gmsh gate。 |
| C04 | Gmsh OpenCASCADE CAD import and geometry kernel documentation | CAD import geometry healing | meshing | `software/gmsh/assets/C04_cad_import_geometry_healing.yaml` | benchmark candidate | CAD import, healing, and entity-map candidate | STEP/BREP smoke + entity map + healing report | 需要小型可提交或可生成 CAD case；需要 entity loss failure ledger | CAD 成功导入不等于 healing 正确，必须记录 imported/modified/deleted/new entity。 |
| C05 | Gmsh size fields and boundary-layer controls | boundary-layer and size-field meshing | meshing | `software/gmsh/assets/C05_boundary_layer_size_field_meshing.yaml` | benchmark candidate | target physical groups, size fields, near-wall mesh diagnostics candidate | size-field schema + boundary-layer validation matrix | 依赖 C02 group contract；不声称 CFD y+ 合格 | 解决 CFD/FEM 常见 near-wall 或 local feature refinement 需求。 |
| C06 | Gmsh mesh formats plus downstream solver import candidates | multi-solver mesh export contract | meshing | `software/gmsh/assets/C06_multi_solver_mesh_export_contract.yaml` | benchmark candidate | export/import matrix candidate | OpenFOAM + FEM-oriented import smoke | 依赖 C02/C03；至少两个 solver family 通过前不提升 benchmark_validated | 把 C01 的 OpenFOAM import smoke 泛化为多求解器 consumability contract。 |
