# Gmsh examples_index

本文件登记已经转化或待转化为科学能力资产的 Gmsh 官方文档、tutorial、benchmark 和本地 case 来源。

| ID | 来源 | capability | domain | 资产卡 | benchmark 状态 | evidence scope | next gate | known blockers | 备注 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| C01 | Gmsh reference manual and official `t1.geo` tutorial | parametric geometry and mesh generation | meshing | `software/gmsh/assets/C01_parametric_geometry_mesh_generation.yaml` | package skeleton created | official documentation plus local schema/config/package dry-run contract, OpenFOAM import smoke, and potentialFoam solve smoke | mesh-quality perturbation and solver-ready regression | 缺少扰动矩阵、更完整 mesh-quality 回归和 benchmark 级 CFD 精度证据 | 已建立 `.geo` 生成、Python API runtime、mesh summary、validation contract、OpenFOAM.com v2112 `gmshToFoam` 导入和 `potentialFoam -writep` 求解 smoke；不提前提升为已验证能力。 |
| C02 | Gmsh physical groups and elementary entity documentation | boundary physical group contract | meshing | `software/gmsh/assets/C02_boundary_physical_group_contract.yaml` | package skeleton created | schema/config/package dry-run contract, negative validation, registry static-ready dispatch, and C01 OpenFOAM import replay smoke | fresh downstream import command using C01/C02 contract | replay 不是重新执行 OpenFOAM；需要 renamed boundary 和 solver-specific alias 负例 | 作为 C03/C05/C06 的边界语义底座，避免下游 solver 依赖隐式 patch 名称。 |
| C03 | Gmsh mesh-size fields, algorithms, and quality controls | mesh refinement quality trend | meshing | `software/gmsh/assets/C03_mesh_refinement_quality_trend.yaml` | package skeleton created | schema/config/package dry-run refinement matrix, quality trend checks, negative validation, and Gmsh Python API refinement smoke | actual perturbation matrix regression | 已生成三档实际 mesh；尚未形成更宽扰动矩阵 | 先验证 mesh 本身，再交给 solver；不把 solver 物理误差塞进 Gmsh gate。 |
| C04 | Gmsh OpenCASCADE CAD import and geometry kernel documentation | CAD import geometry healing | meshing | `software/gmsh/assets/C04_cad_import_geometry_healing.yaml` | package skeleton created | schema/config/package dry-run CAD import manifest, entity map, healing report, meshability checks, negative validation, and generated BREP export/re-open smoke | external or generated STEP/BREP healing benchmark | 当前是 generated BREP smoke；需要真实 entity tag parser 和 entity loss failure ledger | CAD 成功导入不等于 healing 正确，必须记录 imported/modified/deleted/new entity。 |
| C05 | Gmsh size fields and boundary-layer controls | boundary-layer and size-field meshing | meshing | `software/gmsh/assets/C05_boundary_layer_size_field_meshing.yaml` | package skeleton created | schema/config/package dry-run size-field manifest, boundary-layer summary, near-wall metric checks, negative validation, and Distance/Threshold runtime smoke | boundary-layer field runtime + downstream wall validation handoff | 已执行 distance/threshold field runtime；不声称 CFD y+ 合格 | 解决 CFD/FEM 常见 near-wall 或 local feature refinement 需求。 |
| C06 | Gmsh mesh formats plus downstream solver import candidates | multi-solver mesh export contract | meshing | `software/gmsh/assets/C06_multi_solver_mesh_export_contract.yaml` | package skeleton created | schema/config/package dry-run export manifest, format matrix, solver import summary contract, negative validation, OpenFOAM replay, and FEM-oriented `.msh` fixture smoke | fresh OpenFOAM command + true FEM solver import smoke | FEM target 当前是 fixture parser，不是 FEniCSx/CalculiX runtime | 把 C01 的 OpenFOAM import smoke 泛化为多求解器 consumability contract。 |

## 2026-07-02 P1 runtime promotion note

- C02/C06 的 OpenFOAM observation 已改为消费本轮重新执行的 `openfoam_import_wsl_v2112/downstream_import_summary.json`，不再指向旧时间戳 evidence。
- C04 新增 generated STEP import/re-open smoke；BREP 与 STEP 均为 generated fixture，不代表外部 CAD benchmark。
- C06 FEM target 已从 Gmsh mesh summary fixture 提升为 `meshio_fem_import`，但仍不声明 FEniCSx/CalculiX solver runtime。
